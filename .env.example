# 복사해 .env 로 쓰거나, 저장소의 .env 를 그대로 사용하세요.
# PostgreSQL / Prisma / NextAuth 는 사용하지 않습니다. DB 역할은 PocketBase만 합니다.

# PocketBase (GL 서버 등). Admin 계정으로 서버에서만 접근합니다.
POCKETBASE_URL="http://127.0.0.1:8090"
POCKETBASE_ADMIN_EMAIL="admin@example.com"
POCKETBASE_ADMIN_PASSWORD="change-me"

SESSION_SECRET="개발용-32자이상랜덤문자열로교체하세요"

# HTTP 수신 포트 — run-prod.mjs·PM2 기동 시 필수. 프로덕션은 배포가 할당·Caddy 업스트림과 동일하게.
# 로컬·수동 실행 시에만 예: PORT=3000 / npm start 는 셸에서 PORT 지정.
# PORT=

# 내부 전용 단일 업체: PocketBase sabok_tenants 레코드 id. 설정 시 업체 선택·업체 관리 메뉴 숨김, 로그인 시 항상 이 업체.
# SABOK_SINGLE_TENANT_ID=

# HTTPS(Caddy 등)로만 접속하면 1. 내부 HTTP만 쓰면 비우거나 0
# COOKIE_SECURE=1

# pb:seed 가 만드는 로그인 3계정(admin/senior/junior@reversep.local)의 공통 비밀번호.
# 비우면 기본 Reversep2026! — 운영에서는 여기 고정해 두고 시드 재실행으로 초기화하면 됩니다.
# SABOK_SEED_PASSWORD=

# PM2 기동 시마다 pb:seed 돌리려면 1 (기본은 안 함 — 수동 npm run pb:seed 권장)
# SABOK_RUN_SEED_ON_START=1
